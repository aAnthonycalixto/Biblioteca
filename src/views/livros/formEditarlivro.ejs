<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/form.css">
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Editar Livro</title>
</head>
<body>
    <div class="container pt-10 border p-5 my-5 w-50">
        <h1 class="h1 d-flex justify-content-center align-items-center">Editar Livro</h1>
        <div class="d-flex justify-content-center" id="form">
            <form action="/livros/alterar/<%= item.id_livro %>" method="POST">
                <input class="pt-10 border p-2 my-2 w-100" type="text" name="titulo" placeholder="Título" value="<%= item.titulo %>">
                <input class="pt-10 border p-2 my-2 w-100" type="text" name="autor" placeholder="Autor" value="<%= item.autor %>">
                <input class="pt-10 border p-2 my-2 w-100" type="text" name="ano" placeholder="Ano" value="<%= item.ano %>">
                <select class="pt-10 border p-2 my-2 w-100" name="statos">
                    <option value="Disponível" <%= item.statos === 'Disponível' ? 'selected' : '' %>>Disponível</option>
                    <option value="Emprestado" <%= item.statos === 'Emprestado' ? 'selected' : '' %>>Emprestado</option>
                </select>
                <div class="d-flex justify-content-center align-items-center">
                    <button class="bg-primary text-white" type="submit">ATUALIZAR</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
